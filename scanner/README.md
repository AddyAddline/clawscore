# ClawScore Scanner

Run locally:

```bash
./scan.sh --local
```

Upload result immediately:

```bash
./scan.sh --yes
```

Anonymous upload (no machine tracking hash):

```bash
./scan.sh --yes --anonymous
```
